# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/
.eggs/
# sdist/wheel extraction output (e.g. guardops-0.10.1/) — never commit
/guardops-*/

# Environment
.env
*.env.local

# GuardOps runtime files
.guardops-state.json
security/reports/
*.log

# Testing
.coverage
.pytest_cache/
htmlcov/
.mypy_cache/

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Kubernetes (contains cluster credentials — never commit)
kubeconfig
*.kubeconfig

helm.zip

terraform.tfvars
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfstate
terraform.tfstate.backup