# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/
.eggs/
# sdist/wheel extraction output (e.g. guardops-0.10.1/) — never commit
/guardops-*/

# Environment
.env
*.env.local

# GuardOps runtime files
.guardops-state.json
security/reports/
security/metadata/          # Phase 12 scan-metadata SQLite DB
*.log

# Testing
.coverage
.pytest_cache/
htmlcov/
.mypy_cache/

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Kubernetes (contains cluster credentials — never commit)
kubeconfig
*.kubeconfig

helm.zip

terraform.tfvars
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfstate
terraform.tfstate.backup

# Personal project report / interview notes — documentation, not shipped code
GUARDOPS_REPORT.md
# Local DNS recovery artifact (one-off Route53 change-batch)
dashboard-dns.json

# Claude Code local tooling config (editor/agent settings, not part of the product)
.claude/

# Scan-metadata databases — build artifacts that mutate on every run
**/guardops.db
security/metadata/*.db