-r requirements.txt
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
ruff>=0.1.0
mypy>=1.7.0
semgrep>=1.60.0
bandit>=1.7.7
# Phase 13 — dashboard backend (so the dashboard API + auth tests run in CI)
# Pinned near the tested versions; fastapi>=0.116 unblocks a patched starlette
# (CVE-2025-54121 / CVE-2025-62727 / PYSEC-2026-161). Keep in sync with pyproject.
fastapi>=0.136,<0.137
starlette>=1.2,<1.3
uvicorn[standard]>=0.30
httpx>=0.27